export enum UserRole {
    ADMIN = 'admin',
    MEMBER = 'member'
}

export enum UserStatus {
    ONLINE = 'online',
    AWAY = 'away',
    DND = 'dnd',
    OFFLINE = 'offline'
}

export enum MessageType {
    TEXT = 'text',
    FILE = 'file',
    SYSTEM = 'system'
}