export enum UserRole {
  ADMINISTRATOR = "administrator",
  MODERATOR = "moderator",
  MEMBER = "member",
  SECRETARY = "secretary",
}

export enum UserStatus {
  ONLINE = "online",
  AWAY = "away",
  DND = "dnd",
  OFFLINE = "offline",
}

export enum MessageType {
  TEXT = "text",
  FILE = "file",
  SYSTEM = "system",
}
